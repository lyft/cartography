{
  "statements": [
    {
      "query": "MATCH (:KandjiDevice)<-[hs:HAS_SERIAL]-(:CrowdstrikeHost) WHERE hs.lastupdated <> $UPDATE_TAG WITH hs LIMIT $LIMIT_SIZE DELETE (hs) return COUNT(*) as TotalCompleted",
      "iterative": true,
      "iterationsize": 100,
      "__comment__": "Delete relationships between KandjiDevice and CrowdstrikeHost"
    }
  ],
  "name": "Cleanup Kandji relationships with Crowdstrike"
}
