{
    "name": "Autoscale group has launch template",
    "statements": [
        {
            "__comment": "Create HAS_LAUNCH_TEMPLATE relationship from autoscale group to launch config",
            "query":"MATCH (aa:AWSAccount)-[:RESOURCE]->(a:AutoScalingGroup)\nWITH a, aa\nMATCH (aa)-[:RESOURCE]->(lt:LaunchTemplate{id: a.launchtemplateid})\nMERGE (a)-[:HAS_LAUNCH_TEMPLATE]->(lt)",
            "iterative": false
        }
    ]
}
